services:
  fb-comment-bot:
    build: .
    container_name: fb-comment-telegram-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/data
      - ./state:/state
    # Chromium headless cần shared memory lớn hơn default 64MB
    shm_size: 1gb
